export interface Preparation{
    username: string,
    producer_name: string,
    accelerating_time: String,
    type: string,
    name: string, 
    available_quantity: string, 
    unit_price: string
}